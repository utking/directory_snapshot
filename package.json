{
  "name": "directory_snapshot",
  "version": "1.0.0",
  "description": "creates a listing file for a given directory",
  "main": "index.js",
  "repository": "https://github.com/utking/directory_snapshot.git",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "snapshot"
  ],
  "author": "UtkinG",
  "license": "ISC",
  "dependencies": {
    "is-directory": "^0.3.1"
  },
  "devDependencies": {
    "commander": "^2.9.0"
  }
}
